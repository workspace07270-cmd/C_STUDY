# Day 09 문제

## 1. 기본 - 포인터로 변경

`void double_value(int *value)`를 작성해 호출한 변수의 값을 두 배로 변경하세요. `NULL`이면 아무 작업도 하지 않습니다.

## 2. 응용 - 두 출력값

두 정수를 받아 작은 값과 큰 값을 포인터 매개변수로 돌려주는 함수를 작성하세요.

## 3. 도전 - 포인터 추적표

변수 2개와 포인터 2개를 선언해 포인터가 가리키는 대상을 여러 번 바꾸세요. 각 단계의 주소와 역참조 값을 표로 기록합니다.
